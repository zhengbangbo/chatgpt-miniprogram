<view class="textarea-example">
  <t-textarea
    placeholder="请在这输入文字"
    maxlength="{{maxLength}}"
    autosize
    indicator
    model:value="{{content}}"
    disableDefaultPadding="{{true}}"
    bindchange="onChange"
  />
</view>
