<t-tab-bar value="{{selectedValue}}" bindchange="onChange" theme="tag" split="{{false}}">
  <t-tab-bar-item style="font-size: 1rem;" wx:for="{{list}}" wx:key="index" value="{{item.value}}" >
    {{item.label}}
  </t-tab-bar-item>
</t-tab-bar>