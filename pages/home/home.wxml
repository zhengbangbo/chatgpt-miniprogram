<view class="main">
    <view class="title-wrap" bindtap="handleTapLogo">
        <image class="title-icon {{extraClasses}}" bindtransitionend="transitionEnd" mode="aspectFit" src="/assets/logo.png" aria-label="Logo" />
    </view>
    
    <view class="desc">
        <textarea value="{{about}}" auto-height disabled/>
        <view> {{ versionInfo }} </view>
    </view>
</view>