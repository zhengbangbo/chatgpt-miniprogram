/* pages/tool/tool.wxss */
.tool-container {
    display: flex;
    flex-direction: column;
    padding-bottom: 40rpx;
}

textarea {
    min-width: 100%;
    font-size: var(--messages-font-size);
    line-height: var(--messages-font-size);
}

textarea::placeholder {
    font-size: var(--messages-font-size);
    line-height: var(--messages-font-size);
}

.title {
    font-size: 60rpx;
    padding: 12rpx 32rpx 32rpx 32rpx;
}

.description {
    font-size: 32rpx;
    padding: 12rpx 32rpx 32rpx 32rpx;
}

.button-group {
    display: grid;
    grid-template-columns: 1fr 2fr;
}

@keyframes move {
    0% {
        transform: translate(0px, 0px);
    }
    50% {
        transform: translate(0px, 5px);
    }
    100% {
        transform: translate(0px, 0px);
    }
}

.share-note {
    color: gray;
    font-size: 14px;
    margin-right: 2px;
    position: absolute;
    right: 0;
    animation: move 1s infinite;
}

.result-item {
    max-width: 100%;
    margin: 12rpx 32rpx 0 32rpx;
    padding: 20rpx;
    background-color: white;
    border-radius: 10rpx;
}
