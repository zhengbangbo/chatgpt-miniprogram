<view class="textarea-example">
  <t-textarea
    placeholder="请在这输入文字"
    maxlength="140"
    autosize
    indicator
    value="{{askText}}"
    disableDefaultPadding="{{true}}"
    bindchange="onChange"
  />
</view>
